# AI珞（爱珞）V3 仓库状态与路线图

本文件承载会频繁变化的实现状态、历史基线和工作顺序。长期工程约束只放在根目录 `AGENTS.md`。

## 状态口径

- 当前仓库已经实现可靠的校巴垂直链路，并证明 Go → Executor → Capability → Service → Tool → storage → SSE 路径；Python Executor 实现位于 `packages/agent`。
- 2026-07-26 审计 P0 清单已关闭，之后的地基模块也有持续测试证据；这不等于整个平台已经 production-ready。
- 文档中的“设计”不代表实现；只有代码、迁移、测试和部署验收同时存在时才可称“已实现并验证”。
- 真实校方身份、数据和部署边界仍需要独立授权与验收。

## 当前实现基线

### 内核与执行者

- Go 管理 Echo/Run 持久状态机、固定 worker 调度、lease、恢复、取消、重试、App 容量与事件序号。
- Python 使用真实 OpenAI-compatible 原生 ToolCall；Go 执行精确 Capability 投影、Schema 复验、权限收窄、幂等、副作用治理和审计。
- Provider 具备 readiness、超时、取消、限流、并发限制、退避、Token/输出/成本预算和稳定公共错误。
- `packages/agent` 是与业务包同级的 `isolated` / `executor` 包；统一 Loader 管理安装目录中的 hosted、capability 和 executor 组件。

### Service、Tool 与 Loader

- `campus` 是经包管理器安装的 hosted wasm32-wasi 包（源码位于 `packages/campus-bus`，安装目录装载）；Dispatcher → Loader → WasmHost → guest → host function → Go-managed storage 链路已实现。
- **通用包存储 `internal/kernel/packstore`**：`ailuo.store` 宿主函数（get/list/put/delete）+ 窄端口（SQLite/内存实现）；作用域强制为（App × 包 namespace），get/list 内嵌单事务一致的快照元数据（并发快照替换下 reader 永不观察跨修订混合），快照原子替换仅可信 Go 侧开放。**新增有状态包零内核改动**（声明 `[storage]` + 函数子集即可）。
- guest 领域模型与治理逻辑自包含在包仓库（只依赖标准库，2026-08-30 移除 `pkg/bus` 中立包）；数据新鲜度/权威性校验在 guest（读取响应内嵌元数据）。
- hosted Runtime 具备内存和执行时间预算、WASI 裁剪、调用隔离、严格 JSON 信封与稳定错误映射。
- isolated Runtime 在 Linux 使用资源限制，在 Windows 使用 Job Object；不支持的强安全语义 fail closed。
- 外部 `runtime-host` 已实现真实 Backend 与协议接线；声明内核 host function 的安装包仍只能由内核进程内的 WasmHost 执行。

### 包管理器（Package/Component 模型）

- **独立 contracts module**：`contracts/pkg/capability`、`contracts/pkg/packagecontract`、`contracts/pkg/packageio` 和 `contracts/pkg/projectcontract` 提供 Core 与外部包共同消费的稳定契约；其中 `projectcontract` 定义项目依赖清单与 `ailuo.lock` 校验；不依赖 Core `internal`。
- **独立 Package Manager module**：`package-manager/pkg/packagefmt`、`package-manager/pkg/packmgr`、`package-manager/pkg/projectmgr`、`package-manager/pkg/sdkgen` 与 `package-manager/cmd/ailuo-pm` 提供作者侧源清单、项目依赖闭包同步、构建、安装/升级/发布、SDK 生成和 CLI；Core 生产代码不导入包管理器实现。
- 构建器按组件声明选择 `go-wasm`、`go-native`、`python-uv` 或 `ts-as`；包管理器不读取或注入任何具体执行者的配置。
- **Package = 分发/版本/升级单位，Component = 运行单元**（每组件一种 mode，`hosted`/`isolated` 命名暂不改）；组件间经统一 Capability 通信，依赖拓扑决定启动/停止顺序；双权限轴（Capability 逐调用 authZ + 组件沙箱权利）。
- **Loader 组编排**：Catalog 按组件产出内核记录（Capability 按组件 exports 映射到组件运行时，Service/Tools 路由到包内唯一主能力组件）；`Manager.RegisterPackage`/`UpgradePackage`——候选按依赖拓扑加载、全部健康后组级原子切换、旧版本在途全部排空后反序停止（消费者先停、Provider 后停）、任一候选失败不切换。
- **CLI**：`ailuo-pm sync`（项目 `ailuo.toml` → 完整依赖闭包 → `ailuo.lock`）、`install`（作者源目录 / tarball / GitHub Release `owner/repo[@约束]`）、`upgrade`、`uninstall`、`list`、`pack`、`publish`（GitHub Releases 分发后端，纯 REST 免 git CLI，版本不可变）。
- **发布流水线**：可复用 `.github/workflows/package-publish.yml`（按清单构建 → 必填测试 → tag 与 manifest.version 一致性 → pack → tarball 安装冒烟 → 上传 artifact → tag 时 publish → 注册表安装闭环验证）；调用方必须提供受信 Package Manager 的完整 `package-manager-ref` 和 `test-command`，workflow 由 `ailuo-pm inspect` 从清单推导构建器，Package Manager 仓库与模块路径由 workflow 固定；输出 artifact ID，写权限的 publish job 只发布已验证工件，不执行调用方命令；当前仓库未配置 tag caller，包管理器库测试覆盖打包与安装路径。

### Access、身份与上下文

- Web、统一 ingress 和 QQ OneBot v11 适配器均通过标准 `internal/access.Hub` 接入，不直接创建 Echo 或写业务表。
- Deployment User、外部身份、AppMembership、禁用状态和版本化绑定已经持久化并按 App 隔离；不存在匿名降级。
- direct/group/system Session、Message、BlobStore 沙箱、平台消息去重和受限历史读取已经实现。
- QQ 支持群/私聊 allowlist、自动开通受限身份、群聊 @ 过滤、快速回复和戳一戳；Executor 内部递归不产生额外平台生命周期事件。
- Go 的 context assembler 组合当前消息、受限历史和 Capability 投影为中性 Context Block，并持久化确定性摘要与来源版本。

### 配置与治理

- App 配置使用不可变 revision + generation CAS，支持即时撤权、App disable、历史 Run 配置恢复与执行者 readiness。
- 9178 loopback WebUI 只配置 App、Executor ID、opaque ExecutorConfig、通用预算和 QQ Access；Executor 的专属配置留在执行者包自己的 Deployment。
- Confirmation 五态状态机、参数摘要绑定、Dispatcher 校验和确认过期持久任务已经实现。
- 通用持久 Task 状态机支持类型注册、参数 Schema、lease、续期、恢复、有界重试和 App 容量。

### 存储与可观测性

- SQLite 统一存储使用连续前向迁移、App-scoped 复合键、严格约束、备份、完整性校验和无覆盖恢复。
- 当前 Schema 迁移为 1–27：24 是历史 Run 输入列的兼容步骤，25 将包文档快照统一为通用存储，26 将 App/Run 转为 Executor 中性字段，27 归一化旧错误分类；历史迁移只用于前向升级，已应用迁移不改写。
- 校巴快照导入具备来源、权威性、完整性、有效期、引用和原子激活治理；Core 不播种业务演示数据，测试夹具中的 Demo 快照不进入生产启动路径。
- Go/Python 使用统一中文结构化日志、W3C trace 传播、稳定低基数字段、Prometheus 指标和集中净化。
- 公共 HTTP/SSE/Executor 错误不返回原始内部原因；消息、Tool 参数、ExecutorConfig 正文和敏感正文不进入普通日志。

## 关键历史节点

- 2026-07-26：关闭初始审计 P0，完成持久 Echo/Run、Provider、协议、App 配置、存储与运维门禁。
- 2026-08-12：完成身份、会话、附件、Confirmation、Task 和迁移注册表地基。
- 2026-08-13：完成多入口归一、hosted/isolated 统一 Loader、Agent Service 化、上下文装配、执行者协议中性化、Web/QQ 接入。
- 2026-08-14：清理无生产调用者和重复抽象，保留有测试支撑的 PRD 契约。
- 2026-08-15：完成 9178 配置控制面与 V2 配置迁移；执行递归由具体 Executor 自行负责，不进入 Core Run 模型。
- **2026-08-22/23**：包管理器体系——公共契约层 `packagecontract` 与 `packmgr` 安装实现分离、Package/Component 模型（分发与运行单元分离）、Loader 组编排（拓扑启动/原子切换/反序 drain）、CLI（install/upgrade/uninstall/list/inspect/pack/publish）、GitHub Release 分发后端、可复用发布流水线。
- **2026-08-23**：包管理器作者侧源清单——`ailuo.toml`（Cargo.toml 风格继承式精简：tool 以表键为 id、capability 只引用 tool、service 缺省自动生成）经 `package-manager/pkg/packagefmt` 解析为中性清单；`ailuo pack`/`publish` 优先源清单并生成 lock.json；参考 hosted 包迁移为源清单（删除手写 manifest.json）。
- **2026-08-24**：包管理器真正安装（campus 纯安装目录）——`[component.build]` 通用构建声明（内置 go-wasm 构建器，跨平台无 shell 脚本）；校巴 guest 自包含且只依赖标准库，不再依赖内核 `internal/`；安装目录 hosted 包声明 host_functions 由进程内 WasmHost 装载（GRPCHost 拒绝跨进程投影）；内置 campus 生产路径移除 + 启动 fail-closed；campustest 仅保留测试夹具。
- **2026-08-24**：消费方 SDK——`package-manager/pkg/sdkgen` 从包契约（extensions 段 + JSON Schema）生成 Go/Python/TypeScript SDK（三层分离：契约解码 → schema→type 编译 → 逐语言 emitter，仅标准库 + `iancoleman/strcase`）；消费侧端点 `POST /api/v1/capabilities/{id}/invoke`（鉴权 + Dispatcher 统一治理 + 稳定错误映射，OpenAPI 同步）；端到端测试：生成的 Go/Python/TypeScript SDK 各自经 httptest 调用真实 hosted campus capability 返回正确行程。
- **2026-08-24**：包管理器与 Core 的代码边界开始分离——包管理器实现从 Core 内部代码移出；campus.bus 移出 Core 实现目录到 `packages/campus-bus`（独立交叉编译验证，经 Package Manager CLI 分发）；`ailuo sdk-go/sdk-py/sdk-ts` CLI。当前独立 module 布局见 2026-09-01 节点。
- **2026-09-01**：收敛作者侧清单：所有包必须提交显式 `ailuo.toml`；`ailuo-pm` 不再从目录或源码推断包身份、Capability、Schema 或版本，缺少清单直接失败。
- **2026-08-30**：**通用包存储与包零内核改动**——`internal/kernel/packstore` 窄端口 + `ailuo.store` 宿主函数（get/list/put/delete：namespace 绑定包声明、App 隔离宿主强制、get/list 内嵌单事务一致快照元数据；快照原子替换仅可信 Go 侧）；宿主函数按清单提供（`WasmHostConfig.HostFunctionsFor`），`loader.Manifest` 携带 `Storage` 声明；SQLite 迁移 25 以通用 `package_documents`/`package_snapshots` 替换 bus 专属关系表（历史数据前向迁移）；删除 `pkg/bus` 与按包定制投影 `campus.HostedFunctions`，campus-bus guest 自包含（stdlib only）并改走 `ailuo.store`；campustest 改为 testdata guest 源码现场编译（不提交 wasm 工件）；Loader 测试契约常量集中、治理上下文夹具去业务化，echo 编排测试以普通 handler + 中性能力覆盖（不再依赖业务包 fixture）。

**2026-08-31**：目录工件递归完整性与 isolated 进程模板已落地；Agent 与 campus-bus 源码进入同仓 `packages/` 工作区，Core 删除内置 Agent 与 Python 源码，Loader 只消费通用 Role/Mode/lock。

**2026-09-01**：Core 不再硬编码校园 App、Capability、安装检查或演示数据；活动 App ID 来自 Deployment 配置，首次能力集合来自 Registry 与项目 `ailuo.lock` 中的已安装包。校巴契约与 hosted 测试装配移至 `testsupport/campus`，生产启动只按项目锁按角色装载包。

**2026-09-01**：contracts 与 Package Manager 拆为两个独立嵌套 Go module；Core 仅依赖 contracts，包管理 CLI 不再由 Core `main.go` 承载；项目 `ailuo.toml`/`ailuo.lock` 由 `ailuo sync` 管理，`packagesource` 只装载锁定且摘要匹配的安装包。

**2026-09-01**：`executor.v1` 升级为通用执行者协议 4.0；线协议只传递治理上下文、执行输入、Capability 投影和通用预算，模型、Provider、系统提示与 Provider 超时不再属于 Core/协议字段。当前 Python Executor 在自身包内解释上下文并读取自身 Deployment 配置。

**2026-09-01**：Workflow/fake Executor 明确仅作为测试替身，未作为产品包或生产运行时保留；Package Manager 的 `inspect`、作者清单升级、归档 lock 校验、安装恢复和跨安装根锁摘要已统一走显式契约，Core Run 不再维护 child Run 生命周期特例。

**2026-09-01**：包格式升级为 v3；isolated 组件必须显式声明进程模板，移除从 entrypoint 推断进程的旧回退；归档锁与安装锁分别由契约层校验，安装器自动生成安装期进程规格。


当前架构与实现基线见 `docs/系统架构说明.md`；包契约见 `docs/包管理器设计.md`；场景边界见 `docs/校巴场景设计.md`；鉴权、日志、运维和数据授权分别见同目录专项文档。

## 生产阻塞项

### P0

当前没有已知未关闭 P0。新的安全、数据完整性、部署边界或可靠性问题按影响重新定级，历史通过不构成永久豁免。

### P1


- 当目标部署规模、并发或高可用要求超出 SQLite 时，需要新的 Go-managed production database adapter。
- 需要取得书面授权并实现智慧珞珈官方数据 ingestion adapter。
- 多入口产品化仍需真实前端、部署认证、运维指标告警和环境级验收。
- 包管理器：更多语言 SDK（WIT 路线可选演进）、自托管注册表、签名信任链、Loader 公共模块抽取与包管理器独立 repo（当前 projectmgr/packmgr/sdkgen 已可复用）。
- Core 托管启动需要 Provider/部署秘密的 isolated Executor 尚无通用 Secret Provider/监督器合同；当前此类 Executor 必须由自身 Deployment 启动，Core 只按 lock 连接。

## 推荐工作顺序

除非用户重新排序：

1. 处理新发现的 P0。
2. 完成部署环境与 hosted package 剩余边界。
3. 完成授权的真实数据 ingestion。
4. 完成真实产品入口、认证与运维验收。
5. 在稳定地基上增加产品 Capability。
6. 包管理器生态：契约形式化 + 多语言生成器 → 自托管注册表 → 外部包作者接入。

不要在没有用户重新分配的情况下实现由他人负责的 Tool/package-management 对比方案。

## 不变的垂直链路

```text
Access request context
  -> App Capability policy
  -> Registry / Dispatcher
  -> Service
  -> reusable Tool
  -> Go-managed storage port
  -> structured result and persisted events
```

Web、QQ、其他 Access、Python Executor、未来数据库适配器和授权 ingestion 都附着在这条链路上，不移动所有权边界。
