# AI珞 Core 项目依赖清单。Core 不在代码中绑定具体包，包由 ailuo sync
# 解析、构建、安装并写入同目录 ailuo.lock。

[project]
id = "ailuo"

[dependencies."agent"]
version = "1.0.0"
path = "packages/agent"

[dependencies."calendar"]
version = "1.0.0"
path = "packages/calendar"

[dependencies."campus"]
version = "1.0.0"
path = "packages/campus-bus"

[dependencies."classroom"]
version = "1.0.0"
path = "packages/classroom"

[dependencies."ecard"]
version = "1.0.0"
path = "packages/ecard"

[dependencies."library"]
version = "1.0.0"
path = "packages/library"

[dependencies."sports"]
version = "1.0.0"
path = "packages/sports"

[dependencies."timetable"]
version = "1.0.0"
path = "packages/timetable"

[dependencies."weather"]
version = "1.0.0"
path = "packages/weather"
